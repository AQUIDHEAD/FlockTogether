We need a starting main menu

### Main Menu

Create `MainMenu.tsx` inside the `src/` folder.

Requirements:

- Title of he game `Flock Together`
- Add `host game` button
- Add `join game` button
- Add `how to play` button
- Add lilly-pads in the pond scene
- Add a "shore line" at the top of the page

## Update 1

Use the Lilly-pad component found in `src/components/Lilly-pad.tsx` and add some of them to the main menu

Requirements:

- 3 lilly-pads added to the main menu screen
- No other alterations are done